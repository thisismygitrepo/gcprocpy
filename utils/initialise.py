
import numpy as np
import matplotlib.pyplot as plt
import crocodile.toolbox as tb


def init(data_list, config, transfer=None):

    # if config.verbose:
    #     print(f"Initialising data with : {config.init}")

    params = tb.Struct()

    for item in data_list:
        pass


